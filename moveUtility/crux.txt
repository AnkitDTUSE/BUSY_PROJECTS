Utility starts from 

-> main
    1. accepts Args
    2. retrive Source, Dest
    3. check validity of Source, Dest by os.starts
    4. initialize a fileSize variable to store totalFileSize of the data transfer
    5. record time
    6. call MoveUti(Source,Dest,&fileSize)

-> MoveUtil func File
    1. make a global MUTEX lock for fileSize
    2. Jump in MoveUtil func
    3. retirve the basePath of Soucre 
    4. retirve SourceStat from os.Stat()
    5. Check if Source is a Dir or a file using sourceStat.IsDir()
        IF -> source is a file
           6.1 lock the fileSize incerment with mutexLock and unlock it afterwards
           6.2 add basePath to Dest to initialize the fileName of moveing file
           6.3 Open SourceFile and make a file handler
           6.4 check for err free file opening 
           6.5 Create a file at Dest and check for err free file creation
           6.6 create a Buffer (im making a 2MB buffer)
           6.7 Use io.CopyBuffer(dest,source,buffer) to write data in Dest in chunks of 2MB from source 
           6.8 close both Source and Dest file
           6.9 return
        Else -> source is a Dir
           7.1 read the Structure of Source using ReadDir()
           7.2 add base address of sourceDir in Dest
           7.3 create a dir at Dest (the dir basePath which we added in prev step)
           7.4 intialize a waitGroup for Go routines
           7.5 loop over the sourceStructure
           7.6 now Call MoveUtil func for every entry recursively with the base case that source is now a file, in sourceStructure // using Go routines here for concurrent execution of copy files
           7.7 reapeat step 5 until we reach to the depth of the Structure

-> main
    7. record exiting time from MoveUtil
    8. Print details
    9. delete the source using os.Remove()